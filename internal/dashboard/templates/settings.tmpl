{{define "settings_content"}}
<style>
  .settings-container {
    max-width: 900px;
    margin: 0 auto;
    padding: 1.5rem;
  }

  .settings-header {
    margin-bottom: 2rem;
    text-align: center;
  }

  .settings-header h1 {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    color: var(--text);
  }

  .settings-header p {
    font-size: 1rem;
    line-height: 1.6;
    color: var(--muted);
  }

  .settings-icon {
    width: 32px;
    height: 32px;
    stroke-width: 1.5;
  }

  .config-form {
    border-radius: 16px;
    padding: 2rem;
    background: var(--surface);
    border: 1px solid var(--border);
    box-shadow: var(--shadow);
  }

  .form-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  @media (min-width: 768px) {
    .form-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .form-grid .full-width {
      grid-column: 1 / -1;
    }
  }

  .form-field {
    display: flex;
    flex-direction: column;
  }

  .form-field label {
    font-weight: 600;
    margin-bottom: 0.5rem;
    font-size: 0.95rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--text);
  }

  .field-icon {
    width: 20px;
    height: 20px;
    stroke-width: 1.5;
  }

  .form-field input,
  .form-field select {
    padding: 0.75rem 1rem;
    border: 1px solid var(--border);
    border-radius: 8px;
    font-size: 1rem;
    transition: all 0.2s ease;
    height: 48px;
    box-sizing: border-box;
    background: rgba(15, 23, 42, 0.08);
    color: var(--text);
  }

  [data-theme="dark"] .form-field input,
  [data-theme="dark"] .form-field select {
    background: rgba(15, 23, 42, 0.5);
    border-color: rgba(255, 255, 255, 0.12);
  }

  [data-theme="light"] .form-field input,
  [data-theme="light"] .form-field select {
    background: white;
    border-color: rgba(15, 23, 42, 0.15);
  }

  .form-field input:focus,
  .form-field select:focus {
    outline: none;
    border-color: rgba(37, 99, 235, 0.65);
    box-shadow: 0 0 0 4px var(--focus);
  }

  .form-field input:hover,
  .form-field select:hover {
    border-color: rgba(37, 99, 235, 0.4);
  }

  .field-hint {
    margin-top: 0.5rem;
    font-size: 0.85rem;
    line-height: 1.4;
    color: var(--muted);
  }

  .info-card {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border-radius: 12px;
    padding: 1.25rem;
    margin-top: 1.5rem;
    color: white;
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  }

  .info-card-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 600;
    font-size: 1rem;
    margin-bottom: 0.5rem;
  }

  .info-card-icon {
    width: 20px;
    height: 20px;
    stroke-width: 2;
  }

  .info-card p {
    opacity: 0.95;
    line-height: 1.5;
    font-size: 0.9rem;
  }

  .submit-button {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 1rem 2rem;
    border: none;
    border-radius: 8px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease;
    box-shadow: 0 4px 6px -1px rgba(102, 126, 234, 0.3);
    width: 100%;
    margin-top: 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
  }

  .submit-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 15px -3px rgba(102, 126, 234, 0.4);
  }

  .submit-button:active {
    transform: translateY(0);
  }

  .btn-icon {
    width: 20px;
    height: 20px;
    stroke-width: 2;
  }
</style>


<div class="settings-container">
  <div class="settings-header">
    <h1>
      <svg class="settings-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round">
        <path d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 0 0 2.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 0 0 1.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 0 0 -1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 0 0 -2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 0 0 -2.573 -1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 0 0 -1.065 -2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 0 0 1.066 -2.573c-.94-1.543.826-3.31 2.37-2.37c1 .608 2.296.07 2.572-1.065z" />
        <path d="M9 12a3 3 0 1 0 6 0a3 3 0 0 0 -6 0" />
      </svg>
      Configurações do Dispositivo WhatsApp
    </h1>
    <p>Configure como suas instâncias aparecem na lista de dispositivos conectados.</p>
  </div>

  <form class="config-form" method="post" action="/dashboard/settings" id="settingsForm">
    <div class="form-grid">
      <div class="form-field">
        <label for="platform_type">
          <svg class="field-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round">
            <path d="M3 5a1 1 0 0 1 1 -1h16a1 1 0 0 1 1 1v10a1 1 0 0 1 -1 1h-16a1 1 0 0 1 -1 -1v-10z" />
            <path d="M7 20h10" />
            <path d="M9 16v4" />
            <path d="M15 16v4" />
          </svg>
          Tipo de Plataforma
        </label>
        <select id="platform_type" name="platform_type" required>
          <option value="DESKTOP" {{if eq (index .Data "Config").PlatformType "DESKTOP"}}selected{{end}}>Desktop (Computador)</option>
          <option value="CHROME" {{if eq (index .Data "Config").PlatformType "CHROME"}}selected{{end}}>Chrome</option>
          <option value="FIREFOX" {{if eq (index .Data "Config").PlatformType "FIREFOX"}}selected{{end}}>Firefox</option>
          <option value="SAFARI" {{if eq (index .Data "Config").PlatformType "SAFARI"}}selected{{end}}>Safari</option>
          <option value="EDGE" {{if eq (index .Data "Config").PlatformType "EDGE"}}selected{{end}}>Edge</option>
          <option value="IPAD" {{if eq (index .Data "Config").PlatformType "IPAD"}}selected{{end}}>iPad</option>
          <option value="ANDROID_PHONE" {{if eq (index .Data "Config").PlatformType "ANDROID_PHONE"}}selected{{end}}>Android Phone</option>
          <option value="IOS_PHONE" {{if eq (index .Data "Config").PlatformType "IOS_PHONE"}}selected{{end}}>iPhone</option>
        </select>
        <div class="field-hint">
          Ícone exibido na lista de dispositivos do WhatsApp.
        </div>
      </div>

      <div class="form-field">
        <label for="os_name">
          <svg class="field-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round">
            <path d="M3 4m0 1a1 1 0 0 1 1 -1h16a1 1 0 0 1 1 1v14a1 1 0 0 1 -1 1h-16a1 1 0 0 1 -1 -1z" />
            <path d="M7 8h10" />
            <path d="M7 12h10" />
            <path d="M7 16h10" />
          </svg>
          Nome do Sistema Operacional
        </label>
        <input type="text" id="os_name" name="os_name" value="{{(index .Data "Config").OSName}}" placeholder="ApiMe" required>
        <div class="field-hint">
          Nome do sistema enviado ao WhatsApp.
        </div>
      </div>


    </div>

    <div class="info-card">
      <div class="info-card-header">
        <svg class="info-card-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round">
          <path d="M12 12m-9 0a9 9 0 1 0 18 0a9 9 0 1 0 -18 0" />
          <path d="M12 8l.01 0" />
          <path d="M11 12h1v4h1" />
        </svg>
        Importante
      </div>
      <p>Para aplicar as mudanças, reconecte as instâncias (desconecte e gere novo QR code). Afeta apenas novos pareamentos.</p>
    </div>

    <button type="submit" class="submit-button" id="settingsSubmit">
      <svg class="btn-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round">
        <path d="M6 4h10l4 4v10a2 2 0 0 1 -2 2h-12a2 2 0 0 1 -2 -2v-12a2 2 0 0 1 2 -2" />
        <path d="M12 14m-2 0a2 2 0 1 0 4 0a2 2 0 1 0 -4 0" />
        <path d="M14 4l0 4l-6 0l0 -4" />
      </svg>
      Salvar Configurações
    </button>
  </form>
</div>
<script>
document.addEventListener('DOMContentLoaded', function() {
  const settingsForm = document.getElementById('settingsForm');
  const settingsSubmit = document.getElementById('settingsSubmit');
  if (settingsForm && typeof preventDoubleSubmit === 'function') {
    preventDoubleSubmit(settingsForm, { button: settingsSubmit, loadingText: 'Salvando...' });
  }
});
</script>
{{end}}
