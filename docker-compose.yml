services:
  api:
    build: .
    container_name: apime-api
    command: sh -c "./migrate -with-seeds && ./api"
    env_file: .env
    ports:
      - "8081:8081"
    volumes:
      - apime_data:/app/data
    restart: unless-stopped

volumes:
  apime_data: